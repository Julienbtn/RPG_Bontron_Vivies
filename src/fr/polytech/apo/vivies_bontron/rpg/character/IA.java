package fr.polytech.apo.vivies_bontron.rpg.character;


public class IA extends Player {
    Character ia;
    Character player;
    
    
    public IA() {
        this.ia = ia;
    } 
    
    /* public type iaact() {
    if(ia.getHealth() <= (25/100) * maxHEALTH)
            ia.heal(ia, ia); // soigne 25% des pv. Peut être utilisé autant de fois que l'ia a de potion. Ou utilise une potion.
        if((26/100) < ia.getHealth() <= (50/100) * maxHEALTH)
            ia.block(ia, ia);    // double la caractéristique défense
        else 
            ia.attack(ia, player); // attaque le héro
     
       
    }*/
    }
    
    
           
}
